<?xml version="1.0"?>
<clickhouse>
    <users>
        <default>
            <!-- Password: default (in plaintext for testing only) -->
            <password>default</password>
            <networks>
                <ip>::/0</ip>
            </networks>
            <profile>default</profile>
            <quota>default</quota>
        </default>
    </users>
</clickhouse>
