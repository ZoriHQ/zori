services:
  postgres-test:
    image: postgres:latest
    container_name: postgres-test
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
    ports:
      - "5434:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 2s
      timeout: 5s
      retries: 10
    volumes:
      - postgres-test-data:/var/lib/postgresql

  clickhouse-test:
    image: clickhouse/clickhouse-server:latest
    container_name: clickhouse-test
    environment:
      CLICKHOUSE_DB: default
    ports:
      - "9000:9000"
      - "8123:8123"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "localhost:8123/ping"]
      interval: 2s
      timeout: 5s
      retries: 10
    volumes:
      - ./scripts/clickhouse-users-test.xml:/etc/clickhouse-server/users.d/test.xml
      - clickhouse-test-data:/var/lib/clickhouse

  nats-test:
    image: nats:latest
    container_name: nats-test
    command: ["-js", "-m", "8222"]
    ports:
      - "4222:4222"
      - "8222:8222"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "localhost:8222/healthz"]
      interval: 2s
      timeout: 5s
      retries: 10

  redis-test:
    image: redis:latest
    container_name: redis-test
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 5s
      retries: 10
    volumes:
      - redis-test-data:/data

volumes:
  postgres-test-data:
  clickhouse-test-data:
  redis-test-data:

networks:
  default:
    name: zori-test-network
